package api

type UpdaterRepository struct {
	// This would hold pointers to db connections/pools in production code
	// For this challenge, I am leaving it empty and serving the local file
	// and file metadata directly
}